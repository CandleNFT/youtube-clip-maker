# YouTube Clip Maker Backend Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=production

# API Keys (Required)
REPLICATE_API_TOKEN=your_replicate_api_token_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Custom endpoint URLs (if using different providers)
# REPLICATE_API_URL=https://api.replicate.com/v1
# ANTHROPIC_API_URL=https://api.anthropic.com/v1

# File Storage (Optional - defaults to local storage)
# In production, consider using cloud storage and configure accordingly
MAX_FILE_SIZE=2GB
CLEANUP_INTERVAL_HOURS=1

# Security (Optional)
# RATE_LIMIT_WINDOW_MS=900000    # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=5      # requests per window
# CORS_ORIGIN=*                  # or your frontend URL

# Logging (Optional)
LOG_LEVEL=info

# Health Check (Optional)
# HEALTH_CHECK_ENDPOINT=/api/health

# Example API Keys Setup:
# 1. Get Replicate API token: https://replicate.com/account/api-tokens
# 2. Get Anthropic API key: https://console.anthropic.com/account/keys
# 3. Copy this file to .env and fill in your actual keys
# 4. Never commit .env to version control!