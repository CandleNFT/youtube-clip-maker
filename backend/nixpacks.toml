# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_18", "ffmpeg-full", "python311", "python311Packages.pip"]
aptPkgs = ["python3-pip"]

[phases.install]
cmds = [
  "npm ci --omit=dev",
  "pip install yt-dlp"
]

[start]
cmd = "npm start"
